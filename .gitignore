# deps
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# env & secrets
.env
.env.local
.env.*.local
infra/cloudflared/*.json
infra/cloudflared/cert.pem

# prisma
apps/backend/prisma/*.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os
.DS_Store

# docker volumes (when mounted locally)
infra/.volumes/

# test
coverage/

# Markdown files — ignore every *.md by default so stray notes / drafts
# do not leak into the repo. Whitelist the documentation we DO want.
*.md
!README.md
!CLAUDE.md
!SERVER_SETUP.md
!infra/cloudflared/README.md
