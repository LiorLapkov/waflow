auto-install-peers=true
strict-peer-dependencies=false
# Flat dependency layout (no .pnpm/ symlinks).
# Makes behaviour predictable inside multi-stage Docker builds: TS/Node always
# find modules at the regular node_modules/<pkg> path.
node-linker=hoisted
