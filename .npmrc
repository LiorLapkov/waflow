auto-install-peers=true
strict-peer-dependencies=false
# Плоская раскладка зависимостей (без .pnpm/ симлинков).
# Делает поведение в Docker-multi-stage предсказуемым: TS/Node всегда находят
# модули по обычному пути node_modules/<pkg>.
node-linker=hoisted
